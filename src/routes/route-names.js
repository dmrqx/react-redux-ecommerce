export const rootPath = '/'

export const bagPath = '/sacola'

export const searchPath = '/busca'

export const catalogPath = '/produtos'

export const defaultCatalogPath = `${catalogPath}/ver-todos`

export const saleCatalogPath = `${catalogPath}/em-promocao`

export const productDetailPath = '/p/:product-slug'

export const buildProductDetailPath = slug => `/p/${slug}`
